// Historical color-picker integration example placeholder.
//
// ComfyUI loads JavaScript files in this directory as extensions. The previous
// demo code registered sample nodes and called importScripts() at runtime, which
// could produce frontend preload/runtime noise. Keep this file as a no-op module
// so stale preload references still resolve cleanly.
export {};

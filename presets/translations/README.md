# GJJ 通用 TSV 翻译库

`zh_en.tsv` 是 GJJ 内部通用中英对照词库。每行两列：

```text
中文	English
```

约定：

- 第一列固定为中文，第二列固定为英文。
- 两列必须用制表符分隔，不使用逗号或空格分隔。
- 空行和 `#` 开头的注释行会被读取器忽略。
- 读取时会按规范化后的中英 key 去重，重复词条保留第一次出现的翻译。
- 短语替换使用长词优先匹配，同一段文本内可以连续命中多个词条。
- Python 侧统一从 `utils.tsv_translation` 读取，避免每个节点单独维护翻译表。
